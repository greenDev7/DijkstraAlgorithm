namespace DijkstraAlgorithm
{
    public static class Surface
    {
        public static double Plane(double x, double y)
        {
            if (x == 0.0 && y == 1.0)
                return 1.0;

            return 0.0;
        }
    }
}
